[project]
name = "courtslicer"
version = "0.1.0"
description = "Flag basketball plays and cut video into clips"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "python-vlc>=3.0.20123",
]

[project.scripts]
court-slicer = "court_slicer:main"

[tool.uv]
package = true
